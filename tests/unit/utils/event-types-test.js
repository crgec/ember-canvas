import eventTypes from 'dummy/utils/event-types';
import { module, test } from 'qunit';

module('Unit | Utility | event types');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = eventTypes();
  assert.ok(result);
});
