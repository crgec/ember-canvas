import hitTest from 'dummy/utils/hit-test';
import { module, test } from 'qunit';

module('Unit | Utility | hit test');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hitTest();
  assert.ok(result);
});
